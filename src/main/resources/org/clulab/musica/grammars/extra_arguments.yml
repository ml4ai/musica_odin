

    - name: ${addlabel}-axis-surface-1
      label: ${label}
      example: "Reverse all the notes in measure 1."
      priority: ${rulepriority}
      action: ${action}
      type: token
      pattern: |
        @original:${mention_type} around @axis:Pitch